# flashSourceMap

This project archives the original 4399 Flash game package, extracts embedded SWF files, and stores decompiled references for later analysis.

`downloads/` is treated as read-only source material. Modified packages should be created under `workspace/` and emitted into `builds/`.
